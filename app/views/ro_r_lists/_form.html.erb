<%= form_for(ro_r_list) do |f| %>
  <% if ro_r_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ro_r_list.errors.count, "error") %> prohibited this ro_r_list from being saved:</h2>

      <ul>
      <% ro_r_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
